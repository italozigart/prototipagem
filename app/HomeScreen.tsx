import React from 'react';
// Importa o componente visual da pasta screens
// Atenção: ajuste os '../' conforme a real distância entre a pasta app e a pasta screens no seu projeto
import HomeScreenUI from '../src/screens/HomeScreen';

export default function HomeScreenRoute() {
  return <HomeScreenUI />;
}